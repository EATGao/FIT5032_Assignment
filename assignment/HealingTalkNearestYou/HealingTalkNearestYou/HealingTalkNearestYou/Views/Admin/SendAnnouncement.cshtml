
@{
    ViewBag.Title = "SendAnnouncement";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Send Announcement</h2>
<script type="text/javascript">
    function SendContent() {
        var sendContent = $("#inputContent").val();
        $("#content").val(sendContent);
        if ($("#content").val() == "" || $("#content").val() == null) {
            alert("You cannot send an email with empty content.");
            return false;
        }

    }
</script>

@using (Html.BeginForm("SendAnnouncement", "Admin", FormMethod.Post,
    new { enctype = "multipart/form-data", onsubmit = "return SendContent()" }))
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        Content:
        <div class="col-md-10">
            <textarea class="form-control" id="inputContent"></textarea>
            <input type="hidden" name="content" id="content" value="" />
        </div>
    </div>

    <div class="form-group">
        <label class="control-label col-md-2" for="Name">Select File</label>
        <div class="col-md-10">
            <input type="file" name="postedFile" class="btn btn-default" accept="application/pdf" />
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Send" class="btn btn-default" />
        </div>
    </div>
}